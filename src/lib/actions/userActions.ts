"use server";

export const signIn = async (data: signInProps) => {
  try {
    // Mutation
  } catch (error) {
    console.error(error);
  }
};

export const signUp = async (data: SignUpParams) => {
  try {
    // Mutation
  } catch (error) {
    console.error(error);
  }
};
